export type UserProfileType = {
  uuid: string;
  name: string;
  profile_image: string;
  is_admin: boolean;
};

export type AuthorType = {
  uuid: string;
  name: string;
  profile_image: string;
};
